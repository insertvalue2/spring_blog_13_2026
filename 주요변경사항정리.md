# 주요 변경사항 정리
## 13단계: 카카오 소셜 로그인 구현
### 개요
카카오 OAuth 2.0을 활용한 소셜 로그인 기능을 구현했습니다. 사용자는 카카오 계정으로 간편하게 로그인할 수 있으며, 최초 로그인 시 자동으로 회원가입이 진행됩니다.

**주요 특징**:
- 카카오 계정으로 간편 로그인
- 최초 로그인 시 자동 회원가입
- 카카오 프로필 이미지 자동 연동
- 로컬 로그인과 소셜 로그인 구분 (OAuthProvider)

---

## 📋 목차 (빠른 이동)

- [1. 전체 흐름 한눈에 보기](#1-전체-흐름-한눈에-보기)
- [2. OAuthProvider enum 추가](#2-oauthprovider-enum-추가)
- [3. User 엔티티에 provider 필드 추가](#3-user-엔티티에-provider-필드-추가)
- [4. 카카오 OAuth 응답 DTO (UserResponse)](#4-카카오-oauth-응답-dto-userresponse)
- [5. 카카오 로그인 콜백 처리 (UserController)](#5-카카오-로그인-콜백-처리-usercontroller)
- [6. 소셜 회원가입 서비스 (UserService)](#6-소셜-회원가입-서비스-userservice)
- [7. 인터셉터 제외 설정 (WebMvcConfig)](#7-인터셉터-제외-설정-webmvcconfig)
- [8. 로그인 화면에 카카오 버튼 추가](#8-로그인-화면에-카카오-버튼-추가)
- [9. 설정 파일 (application.yml)](#9-설정-파일-applicationyml)
- [💡 학생들을 위한 개념 설명](#-학생들을-위한-개념-설명-1)

---

## 1. 전체 흐름 한눈에 보기

```
1. 사용자가 로그인 화면에서 "카카오 로그인" 버튼 클릭
   ↓
2. 카카오 인증 서버로 리다이렉트 (인가 코드 요청)
   ↓
3. 카카오에서 인가 코드(code)를 받아서 /user/kakao로 리다이렉트
   ↓
4. UserController.kakaoCallback() 메서드 실행
   ↓
5. 인가 코드로 Access Token 발급 요청 (카카오 서버)
   ↓
6. Access Token으로 사용자 정보 요청 (카카오 API)
   ↓
7. 사용자 정보로 username 생성 (닉네임_카카오ID)
   ↓
8. 기존 회원인지 확인
   - 없으면: 자동 회원가입 (소셜 회원가입)
   - 있으면: 로그인 처리
   ↓
9. 세션에 사용자 정보 저장
   ↓
10. 메인 페이지로 리다이렉트
```

---

## 2. OAuthProvider enum 추가

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/OAuthProvider.java`

```java
public enum OAuthProvider {
    LOCAL,  // 일반 회원가입
    KAKAO,  // 카카오 로그인
    NAVER,  // 네이버 로그인 (향후 확장용)
    GOOGLE  // 구글 로그인 (향후 확장용)
}
```

**핵심 포인트**:
- 로그인 경로를 구분하기 위한 enum
- `LOCAL`: 일반 회원가입/로그인
- `KAKAO`: 카카오 소셜 로그인
- 향후 다른 소셜 로그인 추가 시 확장 가능

---

## 3. User 엔티티에 provider 필드 추가

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/User.java`

```java
@Enumerated(EnumType.STRING)
@Column(nullable = false) // null 허용 안 함 (필수)
@ColumnDefault("'LOCAL'") // 문자열이므로 작은따옴표 필수!
private OAuthProvider provider;
```

**빌더에서 기본값 설정**:

```java
@Builder
public User(Long id, String username, String password, String email,
            String profileImage, Timestamp createdAt, List<UserRole> roles,
            OAuthProvider provider) {
    // ... 기존 필드 설정 ...
    
    // provider가 없으면(null) -> 'LOCAL'로 설정
    if (provider == null) {
        this.provider = OAuthProvider.LOCAL;
    } else {
        this.provider = provider;
    }
}
```

**핵심 포인트**:
- `@ColumnDefault("'LOCAL'")`: DB 기본값 설정 (일반 회원가입 시 자동으로 LOCAL)
- 빌더에서 null 체크하여 기본값 보장
- 소셜 로그인 사용자는 `KAKAO`로 저장됨

**프로필 이미지 경로 처리**:

```java
/**
 * [이미지 경로 반환 로직]
 * - 소셜 로그인(http로 시작): URL 그대로 반환
 * - 로컬 로그인(파일명): /images/ 경로를 붙여서 반환
 * - 이미지 없음: null 반환
 */
public String getProfilePath() {
    if (this.profileImage == null) {
        return null;
    }
    // http 로 시작하면(소셜 이미지) 그대로 리턴
    if (this.profileImage.startsWith("http")) {
        return this.profileImage;
    }
    // 아니면(로컬 이미지) 폴더 경로 붙여서 리턴
    return "/images/" + this.profileImage;
}
```

**핵심 포인트**:
- 카카오 프로필 이미지는 외부 URL이므로 그대로 사용
- 로컬 업로드 이미지는 `/images/` 경로를 붙여서 반환

---

## 4. 카카오 OAuth 응답 DTO (UserResponse)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserResponse.java`

### 4-1. OAuthToken DTO

```java
/**
 * 카카오 OAuth 토큰 응답 DTO
 * 
 * 카카오 서버에서 받은 JSON 응답을 자동으로 매핑하기 위한 DTO
 * - 카카오 API는 스네이크 케이스로 응답 (token_type, access_token 등)
 * - Java는 카멜 케이스를 사용하므로 @JsonNaming으로 변환
 */
@JsonNaming(value = PropertyNamingStrategies.SnakeCaseStrategy.class)
@Data
public static class OAuthToken {
    private String tokenType;        // 토큰 타입, bearer로 고정
    private String accessToken;      // 사용자 액세스 토큰 값 (카카오 API 호출용)
    private Integer expiresIn;       // 토큰 만료 시간
    private String refreshToken;     // 리프레시 토큰
    private String refreshTokenExpiresIn; // 리프레시 토큰 만료 시간
}
```

**핵심 포인트**:
- `@JsonNaming(SnakeCaseStrategy.class)`: 카카오 API의 스네이크 케이스를 자동으로 카멜 케이스로 변환
- 카카오 API 응답 예시: `{"token_type": "bearer", "access_token": "..."}`
- Java 필드: `tokenType`, `accessToken`

### 4-2. KakaoProfile DTO

```java
/**
 * 카카오 사용자 프로필 응답 DTO
 */
@JsonNaming(value = PropertyNamingStrategies.SnakeCaseStrategy.class)
@Data
public static class KakaoProfile {
    private Long id;                    // 카카오 사용자 ID
    private String connectedAt;         // 카카오 계정 연결 시각
    private Properties properties;      // 사용자 정보 (닉네임, 프로필 이미지 등)
}

@JsonNaming(value = PropertyNamingStrategies.SnakeCaseStrategy.class)
@Data
public static class Properties {
    private String nickname;            // 카카오 닉네임
    private String profileImage;        // 프로필 이미지 URL (큰 이미지)
    private String thumbnailImage;      // 프로필 이미지 URL (작은 이미지)
}
```

**핵심 포인트**:
- 중첩된 JSON 구조를 DTO로 표현
- `Properties`는 내부 클래스로 정의하여 구조화

---

## 5. 카카오 로그인 콜백 처리 (UserController)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserController.java`

### 5-1. 카카오 콜백 메서드 전체 구조

```java
@Value("${oauth.kakao.client-id}")
private String kakao_client_id;

@Value("${tenco.key}") // 임시 비밀번호 키 주입
private String tencoKey;

/**
 * 카카오 로그인 콜백 처리
 * [흐름] 1. 인가 코드 받기 -> 2. 토큰 발급 요청 -> 3. 사용자 정보 요청 -> 4. 로그인/회원가입 처리
 */
@GetMapping("/user/kakao")
public String kakaoCallback(@RequestParam(name = "code") String code, HttpSession session) {
    // ... 구현 내용 ...
}
```

**핵심 포인트**:
- `@Value("${oauth.kakao.client-id}")`: application.yml에서 카카오 클라이언트 ID 주입
- `@Value("${tenco.key}")`: 소셜 로그인 사용자의 임시 비밀번호 (DB NOT NULL 제약 대응)

### 5-2. 1단계: 인가 코드 확인

```java
// 1. 인가 코드 확인 (디버깅용)
System.out.println("1. Kakao 인가 코드 수신 완료: " + code);
```

**카카오에서 리다이렉트된 URL 예시**:
```
http://localhost:8080/user/kakao?code=ABC123XYZ...
```

### 5-3. 2단계: Access Token 발급 요청

```java
// -----------------------------------------------------------
// 2. 토큰 발급 요청 (POST: https://kauth.kakao.com/oauth/token)
// -----------------------------------------------------------
RestTemplate tokenRt = new RestTemplate();

// 2-1. 헤더 생성 (MIME 타입 설정)
HttpHeaders tokenHeaders = new HttpHeaders();
tokenHeaders.add("Content-type", "application/x-www-form-urlencoded;charset=utf-8");

// 2-2. 바디 생성 (MultiValueMap 사용)
MultiValueMap<String, String> tokenParams = new LinkedMultiValueMap<>();
tokenParams.add("grant_type", "authorization_code");
tokenParams.add("client_id", kakao_client_id);
tokenParams.add("redirect_uri", "http://localhost:8080/user/kakao");
tokenParams.add("code", code);
tokenParams.add("client_secret", "AhbShsedonaVOqoMyRgl0sjX6ZXNsbHU");

// 2-3. 헤더 + 바디 결합
HttpEntity<MultiValueMap<String, String>> tokenRequest = new HttpEntity<>(tokenParams, tokenHeaders);

// 2-4. 요청 및 응답 받기
ResponseEntity<UserResponse.OAuthToken> tokenResponse = tokenRt.exchange(
        "https://kauth.kakao.com/oauth/token",
        HttpMethod.POST,
        tokenRequest,
        UserResponse.OAuthToken.class
);

UserResponse.OAuthToken oauthToken = tokenResponse.getBody();
```

**핵심 포인트**:
- `RestTemplate`: Spring에서 HTTP 요청을 보내는 클래스
- `MultiValueMap`: HTML Form 데이터 형식 (key=value&key=value)으로 변환
- `exchange()`: HTTP 요청을 보내고 응답을 받는 메서드
- 응답을 `OAuthToken` DTO로 자동 변환

### 5-4. 3단계: 사용자 정보 요청

```java
// -----------------------------------------------------------
// 3. 사용자 정보 요청 (POST: https://kapi.kakao.com/v2/user/me)
// -----------------------------------------------------------
RestTemplate profileRt = new RestTemplate();

// 3-1. 헤더 생성 (Bearer + Access Token)
HttpHeaders profileHeaders = new HttpHeaders();
profileHeaders.add("Authorization", "Bearer " + oauthToken.getAccessToken());
profileHeaders.add("Content-type", "application/x-www-form-urlencoded;charset=utf-8");

// 3-2. 요청 엔티티 생성 (바디 없음)
HttpEntity<Void> profileRequest = new HttpEntity<>(profileHeaders);

// 3-3. 요청 및 응답 받기
ResponseEntity<UserResponse.KakaoProfile> profileResponse = profileRt.exchange(
        "https://kapi.kakao.com/v2/user/me",
        HttpMethod.POST,
        profileRequest,
        UserResponse.KakaoProfile.class
);

UserResponse.KakaoProfile kakaoProfile = profileResponse.getBody();
```

**핵심 포인트**:
- `Authorization: Bearer {accessToken}`: 카카오 API 인증 헤더
- Access Token을 사용하여 사용자 정보 조회
- 응답을 `KakaoProfile` DTO로 자동 변환

### 5-5. 4단계: 자동 회원가입 및 로그인 처리

```java
// -----------------------------------------------------------
// 4. 강제 회원가입 및 로그인 처리
// -----------------------------------------------------------

// 4-1. 고유한 username 생성 (중복 방지용: 닉네임_카카오ID)
String username = kakaoProfile.getProperties().getNickname() + "_" + kakaoProfile.getId();

// 4-2. 회원 가입 여부 확인 (UserService 활용)
User userOrigin = userService.사용자이름조회(username);

if (userOrigin == null) {
    // 회원가입용 엔티티 생성
    User newUser = User.builder()
            .username(username)
            .password(tencoKey) // 임시 비밀번호 (DB Not Null 제약 대응)
            .email(username + "@kakao.com") // 임의의 이메일
            .provider(OAuthProvider.KAKAO) // ★ 로그인 경로 설정
            .build();

    // 프로필 이미지가 있다면 설정
    String profileImage = kakaoProfile.getProperties().getThumbnailImage();
    if (profileImage != null && !profileImage.isEmpty()) {
        newUser.setProfileImage(profileImage); // URL 그대로 저장 (외부 링크)
    }

    userService.소셜회원가입(newUser);
    userOrigin = newUser; // !! 필수
} else {
    // 이미 가입된 회원입니다. 로그인을 진행합니다.
}

// 4-3. 세션 등록 (로그인 처리)
session.setAttribute("sessionUser", userOrigin);

return "redirect:/";
```

**핵심 포인트**:
- **고유한 username 생성**: `닉네임_카카오ID` 형식으로 중복 방지
- **자동 회원가입**: 기존 회원이 아니면 자동으로 회원가입 진행
- **임시 비밀번호**: DB NOT NULL 제약을 위해 `tencoKey` 사용 (실제로는 사용하지 않음)
- **프로필 이미지**: 카카오 프로필 이미지 URL을 그대로 저장
- **세션 등록**: 로컬 로그인과 동일하게 세션에 사용자 정보 저장

---

## 6. 소셜 회원가입 서비스 (UserService)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

### 6-1. 사용자명 조회 메서드

```java
/**
 * 사용자명으로 조회 (소셜 로그인용)
 */
@Transactional(readOnly = true)
public User 사용자이름조회(String username) {
    // Optional 처리: 없으면 null 반환
    return userRepository.findByUsername(username).orElse(null);
}
```

**핵심 포인트**:
- 소셜 로그인 시 기존 회원 여부 확인용
- 없으면 `null` 반환하여 자동 회원가입 진행

### 6-2. 소셜 회원가입 메서드

```java
public void 소셜회원가입(User user) {
    userRepository.save(user);   // 그냥 저장만 하면 됨
}
```

**핵심 포인트**:
- 일반 회원가입과 달리 유효성 검사나 중복 체크가 필요 없음
- username이 이미 고유하게 생성되었고, 카카오에서 인증된 사용자이므로 바로 저장
- `CascadeType.ALL` 설정으로 `UserRole`도 자동 저장됨 (기본 USER 권한)

---

## 7. 인터셉터 제외 설정 (WebMvcConfig)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/config/WebMvcConfig.java`

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    // LoginInterceptor를 시스템에 등록
    registry.addInterceptor(loginInterceptor)
            .addPathPatterns("/board/**", "/user/**", "/reply/**", "/admin/**")
            .excludePathPatterns(
                    // 로그인 관련 (인증이 필요 없는 페이지)
                    "/login",                    // 로그인 화면 및 처리
                    "/join",                     // 회원가입 화면 및 처리
                    "/logout",                   // 로그아웃
                    "/user/kakao",               // ★ 카카오 리다이렉트 URI 제외
                    
                    // 게시글 조회 관련
                    "/board/list",
                    "/",
                    "/board/{id:\\d+}",
                    
                    // 정적 리소스
                    "/css/**",
                    "/js/**",
                    "/images/**",
                    "/favicon.ico",
                    "/h2-console/**"
            );
}
```

**핵심 포인트**:
- `/user/kakao` 경로를 인터셉터에서 제외
- 카카오에서 리다이렉트된 요청은 로그인 전 상태이므로 인터셉터를 통과해야 함
- 카카오 콜백 처리 후 세션에 사용자 정보를 저장하므로 이후 요청부터는 로그인 상태

---

## 8. 로그인 화면에 카카오 버튼 추가

**파일**: `src/main/resources/templates/user/login-form.mustache`

```mustache
<div class="card-body">
    <form action="/login" method="post" enctype="application/x-www-form-urlencoded">
        <div class="mb-3">
            <input type="text" class="form-control" placeholder="Enter username" name="username">
        </div>
        <div class="mb-3">
            <input type="password" class="form-control" placeholder="Enter password" name="password">
        </div>
        <button type="submit" class="btn btn-primary form-control mb-3">로그인</button>
        <a href="https://kauth.kakao.com/oauth/authorize?response_type=code&client_id=13a5d7c18fd6eaa38324e322a010c50e&redirect_uri=http://localhost:8080/user/kakao">
            <img alt="소셜로그인이미지" src="/img/kakao_login_small.png">
        </a>
    </form>
</div>
```

**카카오 인증 URL 구조**:
```
https://kauth.kakao.com/oauth/authorize?
    response_type=code&
    client_id={카카오_앱_키}&
    redirect_uri={리다이렉트_URI}
```

**핵심 포인트**:
- `response_type=code`: 인가 코드 방식 사용
- `client_id`: 카카오 개발자 센터에서 발급받은 앱 키
- `redirect_uri`: 카카오 인증 후 돌아올 URI (반드시 카카오 개발자 센터에 등록된 URI와 일치해야 함)
- 카카오 로그인 버튼 이미지: `/img/kakao_login_small.png`

---

## 9. 설정 파일 (application.yml)

**파일**: `src/main/resources/application-dev.yml`

```yaml
oauth:
  kakao:
    client-id: ${KAKAO_CLIENT_ID}  # 환경 변수에서 주입

tenco:
  key: ${TENCO_KEY}  # 소셜 로그인 사용자 임시 비밀번호
```

**핵심 포인트**:
- `${KAKAO_CLIENT_ID}`: 환경 변수에서 카카오 클라이언트 ID 주입
- `${TENCO_KEY}`: 소셜 로그인 사용자의 임시 비밀번호 (실제로는 사용하지 않지만 DB NOT NULL 제약 대응)
- 보안을 위해 민감한 정보는 환경 변수로 관리

---

## 💡 학생들을 위한 개념 설명

### 1. OAuth 2.0이란?

**OAuth 2.0**은 **"다른 서비스의 계정으로 우리 서비스에 로그인하는 것"**을 가능하게 하는 인증 프로토콜입니다.

**예시**:
- 카카오 계정으로 우리 블로그에 로그인
- 구글 계정으로 쇼핑몰에 로그인
- 네이버 계정으로 뉴스 사이트에 로그인

**왜 사용하나요?**
- 사용자가 별도로 회원가입할 필요 없음 (편의성)
- 비밀번호를 우리 서버에 저장하지 않아도 됨 (보안)
- 카카오가 인증을 대신 처리해줌 (신뢰성)

---

### 2. OAuth 2.0 인증 흐름 - 인가 코드 승인 방식(Authorization Code 방식)

```
1. 사용자: "카카오로 로그인" 버튼 클릭
   ↓
2. 우리 서버: 카카오 인증 서버로 리다이렉트
   "카카오야, 이 사용자를 인증해줘"
   ↓
3. 카카오: 사용자에게 로그인 화면 보여줌
   ↓
4. 사용자: 카카오 계정으로 로그인
   ↓
5. 카카오: "인가 코드(code)"를 우리 서버로 전달
   "이 코드로 토큰을 받아"
   ↓
6. 우리 서버: 인가 코드로 "Access Token" 요청
   ↓
7. 카카오: Access Token 발급
   ↓
8. 우리 서버: Access Token으로 사용자 정보 요청
   ↓
9. 카카오: 사용자 정보 반환 (닉네임, 프로필 이미지 등)
   ↓
10. 우리 서버: 사용자 정보로 자동 회원가입 또는 로그인 처리
```

---

### 3. RestTemplate이란?

**RestTemplate**은 Spring에서 **HTTP 요청을 보내는 클래스**입니다.

**사용 예시**:
```java
RestTemplate rt = new RestTemplate();

// GET 요청
String response = rt.getForObject("https://api.example.com/data", String.class);

// POST 요청
ResponseEntity<User> response = rt.exchange(
    "https://api.example.com/users",
    HttpMethod.POST,
    requestEntity,
    User.class
);
```

**이 프로젝트에서의 사용**:
- 카카오 토큰 발급 API 호출
- 카카오 사용자 정보 API 호출

---

### 4. @JsonNaming이란?

**@JsonNaming**은 JSON의 필드명과 Java 클래스의 필드명이 다를 때 자동으로 변환해주는 어노테이션입니다.

**문제 상황**:
- 카카오 API 응답: `{"token_type": "bearer", "access_token": "..."}` (스네이크 케이스)
- Java 필드: `tokenType`, `accessToken` (카멜 케이스)

**해결 방법**:
```java
@JsonNaming(value = PropertyNamingStrategies.SnakeCaseStrategy.class)
@Data
public static class OAuthToken {
    private String tokenType;      // token_type → tokenType 자동 변환
    private String accessToken;    // access_token → accessToken 자동 변환
}
```

**핵심 포인트**:
- 카카오 API는 스네이크 케이스를 사용
- Java는 카멜 케이스를 사용
- `@JsonNaming`으로 자동 변환하여 매핑

---

### 5. 왜 username을 "닉네임_카카오ID"로 만들었나요?

**문제**: 여러 사용자가 같은 닉네임을 가질 수 있음

**예시**:
- 사용자 A: 닉네임 "홍길동"
- 사용자 B: 닉네임 "홍길동"
- → username 중복 오류 발생!

**해결**: 카카오 ID를 붙여서 고유하게 만듦

**예시**:
- 사용자 A: "홍길동_123456789"
- 사용자 B: "홍길동_987654321"
- → username이 고유함!

**핵심 포인트**:
- 카카오 ID는 고유하므로 중복 방지 가능
- 사용자가 보기에는 닉네임만 보이고, 실제로는 고유한 username 사용

---

### 6. 왜 임시 비밀번호를 사용하나요?

**문제**: 소셜 로그인 사용자는 비밀번호를 입력하지 않음

**해결**: DB NOT NULL 제약을 위해 임시 비밀번호 사용

```java
.password(tencoKey) // 임시 비밀번호 (DB Not Null 제약 대응)
```

**핵심 포인트**:
- 소셜 로그인 사용자는 비밀번호를 입력하지 않으므로 임시 값 필요
- 실제로는 사용하지 않음 (소셜 로그인으로만 접근)
- DB 스키마의 `password` 컬럼이 NOT NULL이므로 필수

---

### 7. 프로필 이미지 처리 차이

**로컬 로그인**:
- 사용자가 파일을 업로드
- 서버 디스크에 저장 (`images/` 폴더)
- DB에는 파일명만 저장 (예: `abc123-profile.jpg`)
- 화면에 표시: `/images/abc123-profile.jpg`

**소셜 로그인 (카카오)**:
- 카카오에서 프로필 이미지 URL 제공
- DB에 URL 그대로 저장 (예: `https://k.kakaocdn.net/...`)
- 화면에 표시: URL 그대로 사용

**User 엔티티의 getProfilePath() 메서드**:
```java
public String getProfilePath() {
    if (this.profileImage == null) {
        return null;
    }
    // http로 시작하면(소셜 이미지) 그대로 리턴
    if (this.profileImage.startsWith("http")) {
        return this.profileImage;
    }
    // 아니면(로컬 이미지) 폴더 경로 붙여서 리턴
    return "/images/" + this.profileImage;
}
```

**핵심 포인트**:
- 로컬 이미지: 파일명만 저장 → `/images/` 경로 추가
- 소셜 이미지: URL 전체 저장 → 그대로 사용

---

## 주요 파일 변경 목록

1. **`OAuthProvider.java`** (신규)
   - 로그인 경로 구분 enum (LOCAL, KAKAO, NAVER, GOOGLE)

2. **`User.java`** (수정)
   - `provider: OAuthProvider` 필드 추가
   - `getProfilePath()` 메서드 추가 (로컬/소셜 이미지 구분)

3. **`UserResponse.java`** (수정)
   - `OAuthToken` DTO 추가 (카카오 토큰 응답)
   - `KakaoProfile` DTO 추가 (카카오 사용자 정보)
   - `Properties` DTO 추가 (카카오 사용자 속성)

4. **`UserController.java`** (수정)
   - `kakaoCallback()` 메서드 추가 (카카오 OAuth 콜백 처리)
   - `@Value` 어노테이션으로 카카오 클라이언트 ID 주입
   - `@Value` 어노테이션으로 임시 비밀번호 키 주입

5. **`UserService.java`** (수정)
   - `사용자이름조회()` 메서드 추가 (소셜 로그인용)
   - `소셜회원가입()` 메서드 추가

6. **`UserRepository.java`** (변경 없음)
   - 기존 `findByUsername()` 메서드 활용

7. **`WebMvcConfig.java`** (수정)
   - `/user/kakao` 경로를 LoginInterceptor에서 제외

8. **`login-form.mustache`** (수정)
   - 카카오 로그인 버튼 추가

9. **`application-dev.yml`** (수정)
   - `oauth.kakao.client-id` 설정 추가
   - `tenco.key` 설정 추가

---

## 🎯 핵심 정리

### 카카오 소셜 로그인의 핵심

1. **OAuth 2.0 인증 흐름**: 인가 코드 → Access Token → 사용자 정보
2. **자동 회원가입**: 최초 로그인 시 자동으로 회원가입 진행
3. **고유한 username**: 닉네임_카카오ID 형식으로 중복 방지
4. **프로필 이미지 연동**: 카카오 프로필 이미지 자동 연동
5. **로그인 경로 구분**: OAuthProvider로 로컬/소셜 로그인 구분
6. **인터셉터 제외**: 카카오 콜백 URI는 로그인 전 상태이므로 인터셉터 제외
7. **RestTemplate 활용**: 카카오 API 호출을 위한 HTTP 클라이언트

### 실무에서 중요한 점

- **보안**: 클라이언트 ID, 시크릿 키는 환경 변수로 관리
- **에러 처리**: 카카오 API 호출 실패 시 예외 처리 필요
- **확장성**: 다른 소셜 로그인(네이버, 구글) 추가 시 OAuthProvider enum 확장
- **사용자 경험**: 자동 회원가입으로 사용자 편의성 향상
- **데이터 일관성**: 소셜 로그인 사용자도 일반 사용자와 동일한 권한 체계 적용

