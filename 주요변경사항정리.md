# 주요 변경사항 정리

## 12단계: 역할(Role) 기반 권한 관리

### 개요

사용자에게 **역할(Role)**을 부여하고, 역할에 따라 **관리자 전용 메뉴/페이지**에 접근을 제어하는 기능을 구현했습니다.

- `ADMIN`: 관리자 (관리자 대시보드 접근 가능)
- `USER`: 일반 사용자

---

## 📋 목차 (빠른 이동)

- [1. 전체 구조 한눈에 보기](#1-전체-구조-한눈에-보기)
- [2. 엔티티 설계 (Role, UserRole, User)](#2-엔티티-설계-role-userrole-user)
- [3. 로그인 시 권한 정보 함께 조회](#3-로그인-시-권한-정보-함께-조회)
- [4. 관리자 전용 컨트롤러 및 인터셉터](#4-관리자-전용-컨트롤러-및-인터셉터)
- [5. 헤더 메뉴 및 관리자 대시보드 뷰](#5-헤더-메뉴-및-관리자-대시보드-뷰)
- [6. 초기 데이터 (data.sql)](#6-초기-데이터-datasql)
- [7. 예외 처리 개선 (MyExceptionHandler)](#7-예외-처리-개선-myexceptionhandler)
- [8. 회원가입 시 기본 권한 부여](#8-회원가입-시-기본-권한-부여)
- [💡 학생들을 위한 개념 설명](#-학생들을-위한-개념-설명)
- [테스트 시나리오](#테스트-시나리오)
- [주요 파일 변경 목록](#주요-파일-변경-목록)

---

## 1. 전체 구조 한눈에 보기

```
1. 역할 정의: Role enum (ADMIN, USER)
   ↓
2. 역할 매핑 테이블: UserRole 엔티티 (user_role_tb)
   ↓
3. User 엔티티에 roles 필드 추가 (1:N 관계)
   ↓
4. 로그인 시 User + roles를 함께 조회
   ↓
5. 헤더에서 {{#isAdmin}}으로 관리자 메뉴 노출
   ↓
6. /admin/** URI: 로그인 + 관리자 권한만 접근 가능
```

---

## 2. 엔티티 설계 (Role, UserRole, User)

### 2-1. Role enum

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/Role.java`

```java
/**
 * 사용자 권한(역할)을 나타내는 enum.
 * - ADMIN : 관리자
 * - USER  : 일반 사용자
 */
public enum Role {
    ADMIN, USER
}
```

**핵심 포인트**:
- `"ADMIN"`, `"USER"` 문자열 대신 **타입 안전한 enum** 사용
- 오타를 컴파일 타임에 잡을 수 있음

---

### 2-2. UserRole 엔티티

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserRole.java`

```java
@Table(
        name = "user_role_tb",
        uniqueConstraints = {
                @UniqueConstraint(name = "uk_user_role", columnNames = {"user_id", "role"})
        }
)
@Entity
@NoArgsConstructor
@Getter
public class UserRole {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false, length = 20)
    private Role role;

    @Builder
    public UserRole(Long id, Role role) {
        this.id = id;
        this.role = role;
    }
}
```

**핵심 포인트**:
- 테이블: `user_role_tb`
- 유니크 제약 조건: **(user_id, role)** 조합
  - 같은 유저에게 같은 ROLE을 두 번 넣지 못하게 방지
- `@Enumerated(EnumType.STRING)`: DB에 "ADMIN", "USER" 문자열로 저장

**테이블 구조**:
```sql
CREATE TABLE user_role_tb (
    id BIGINT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    role VARCHAR(20) NOT NULL,
    UNIQUE KEY uk_user_role (user_id, role),
    FOREIGN KEY (user_id) REFERENCES user_tb(id)
);
```

---

### 2-3. User 엔티티 역할 필드 추가

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/User.java`

```java
@Entity
@Table(name = "user_tb")
public class User {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true)
    private String username;
    private String password;
    private String email;
    private String profileImage;
/**
     * [사용자 권한 목록]
     * User(1) : UserRole(N)의 관계를 맺습니다.
     *
     * 1. @OneToMany + @JoinColumn(name = "user_id")
     * - User가 UserRole 리스트를 관리합니다. (단방향)
     * - 실제 DB의 'user_role_tb' 테이블에 'user_id'라는 FK 컬럼이 생깁니다.
     *
     * 2. CascadeType.ALL
     * - "운명 공동체"입니다. User를 저장(save)하면 Role도 자동 저장되고, 
     * User를 삭제(delete)하면 가지고 있던 Role들도 다 같이 삭제됩니다.
     *
     * 3. orphanRemoval = true
     * - "리스트와 DB의 동기화"입니다. 
     * Java의 roles 리스트에서 요소(Role)를 .remove() 하거나 .clear() 하면,
     * DB에서도 해당 데이터(DELETE 쿼리)가 실제로 삭제됩니다.
     *
     * 4. fetch = FetchType.EAGER
     * - "즉시 로딩"입니다. User를 조회(select)하는 시점에
     * 무조건 Role 데이터까지 조인(JOIN)해서 한 번에 가져옵니다.
     * (뷰 렌더링 시 LazyInitializationException 방지 목적)
     */
    @OneToMany(cascade = CascadeType.ALL, orphanRemoval = true, fetch = FetchType.EAGER)
    @JoinColumn(name = "user_id") 
    private List<UserRole> roles = new ArrayList<>();

    // ... 기존 필드들 ...
}
```

**핵심 포인트**:
- `fetch = FetchType.EAGER`
  - User를 DB에서 읽어올 때 **항상 roles도 같이 가져옴**
  - 뷰에서 `user.isAdmin()`을 호출할 때 Lazy 로딩 예외 없이 동작
  - **학습용으로 이해하기 쉽게** 설계 (실무에서는 상황에 따라 LAZY도 고려)

---

### 2-4. User의 권한 관련 편의 메서드

```java
// ===================== 권한(ROLE) 관련 편의 메서드 =====================

/**
 * 새로운 역할을 추가합니다.
 * 최초 회원가입 시 기본 USER 역할을 부여하거나,
 * 관리자가 ADMIN 역할을 부여할 때 사용할 수 있습니다.
 */
public void addRole(Role role) {
    this.roles.add(UserRole.builder().role(role).build());
}

/**
 * 해당 역할을 가지고 있는지 확인합니다.
 */
public boolean hasRole(Role role) {
    // roles 컬렉션이 없거나 비어 있으면 해당 역할이 없는 것
    if (this.roles == null || this.roles.isEmpty()) {
        return false;
    }
    // EAGER 로딩을 사용하므로 단순히 stream으로 검사해도 됨
    return this.roles.stream()
            .anyMatch(r -> r.getRole() == role);
}

/**
 * 관리자인지 여부를 반환합니다.
 */
public boolean isAdmin() {
    return hasRole(Role.ADMIN);
}

/**
 * Mustache에서 boolean 프로퍼티로 접근하기 위한 getter.
 * - 템플릿에서 {{#isAdmin}} ... {{/isAdmin}} 형태로 사용할 수 있습니다.
 */
public boolean getIsAdmin() {
    return isAdmin();
}

/**
 * 화면에 표시할 역할 문자열.
 * - ADMIN 이면 "ADMIN"
 * - 그 외(또는 기본)는 "USER"
 */
public String getRoleDisplay() {
    return isAdmin() ? "ADMIN" : "USER";
}
```

**핵심 포인트**:
- `getIsAdmin()`: Mustache에서 `{{#isAdmin}}`으로 사용 가능
  - Mustache는 `getIsAdmin()`을 `isAdmin`이라는 이름으로 인식
- `getRoleDisplay()`: 헤더에서 `ADMIN` / `USER` 배지 표시용

---

## 3. 로그인 시 권한 정보 함께 조회

### 3-1. UserRepository - roles까지 한 번에 조회

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserRepository.java`

```java
/**
 * 로그인 시 역할(ROLE) 정보까지 함께 조회하는 메서드.
 *
 * - LEFT JOIN FETCH 를 사용하여 user_role_tb 를 한 번에 로딩합니다.
 * - 세션에 저장된 User 객체에서 isAdmin(), getRoleDisplay() 등을 사용할 수 있습니다.
 */
@Query("select distinct u from User u left join fetch u.roles r " +
       "where u.username = :username and u.password = :password")
Optional<User> findByUsernameAndPasswordWithRoles(@Param("username") String username,
                                                  @Param("password") String password);
```

**핵심 포인트**:
- `LEFT JOIN FETCH u.roles`
  - 로그인할 때 유저 + 롤 정보를 **한 번에** 가져옴
  - N+1 문제 방지

**생성되는 SQL**:
```sql
SELECT DISTINCT u.*, r.* 
FROM user_tb u 
LEFT JOIN user_role_tb r ON u.id = r.user_id 
WHERE u.username = ? AND u.password = ?
```

---

### 3-2. UserService - 로그인 및 회원가입 로직

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

#### 로그인 메서드

```java
@Transactional(readOnly = true)
public User 로그인(UserRequest.LoginDTO loginDTO) {
    // 1. 유효성 검사
    loginDTO.validate();

    // 2. 사용자명과 비밀번호로 사용자 조회 (+ 역할 정보까지 함께 조회)
    //    findByUsernameAndPasswordWithRoles():
    //    - User 엔티티와 roles 컬렉션을 LEFT JOIN FETCH로 한 번에 가져옵니다.
    //    - 세션에 저장된 User에서 isAdmin(), getRoleDisplay() 등을 사용할 수 있습니다.
    User sessionUser = userRepository.findByUsernameAndPasswordWithRoles(
                    loginDTO.getUsername(),
                    loginDTO.getPassword())
            .orElse(null); // 로그인 실패 시 null 반환

    // 3. 로그인 실패 처리
    if (sessionUser == null) {
        throw new Exception400("사용자명 또는 비밀번호가 올바르지 않습니다");
    }

    return sessionUser;
}
```

**핵심 포인트**:
- 세션에 저장되는 `sessionUser`는 **이미 roles 정보가 채워진 상태**
- 이후 뷰, 컨트롤러, 인터셉터 어디서든 `sessionUser.isAdmin()`을 바로 사용 가능

#### 회원가입 메서드

```java
@Transactional
public User 회원가입(UserRequest.JoinDTO joinDTO) {
    // 1. 유효성 검사
    joinDTO.validate();

    // 2. 사용자명 중복 체크
    if (userRepository.findByUsername(joinDTO.getUsername()).isPresent()) {
        throw new Exception400("이미 존재하는 사용자 이름입니다");
    }

    // 3. 프로필 이미지 저장 (선택사항)
    // ... 프로필 이미지 처리 로직 ...

    // 4. DTO를 엔티티로 변환 (파일명 포함)
    User user = joinDTO.toEntity(profileImageFilename);

    // 5. 기본 권한 추가 (일반 사용자)
    // 회원가입 시 기본적으로 USER 역할을 부여합니다.
    user.addRole(Role.USER);

    // 6. JpaRepository의 save() 메서드: 엔티티 저장 (INSERT)
    return userRepository.save(user);
}
```

**핵심 포인트**:
- 회원가입 시 자동으로 `USER` 역할이 추가됨
- `CascadeType.ALL` 설정으로 `userRepository.save(user)` 호출 시 `UserRole`도 함께 저장됨

---

## 4. 관리자 전용 컨트롤러 및 인터셉터

### 4-1. WebMvcConfig - `/admin/**`도 로그인 필수

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/config/WebMvcConfig.java`

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    // SessionInterceptor를 모든 요청에 등록
    registry.addInterceptor(sessionInterceptor)
            .addPathPatterns("/**");
    
    // LoginInterceptor를 시스템에 등록
    registry.addInterceptor(loginInterceptor)
            // 인터셉터가 동작할 URL 패턴 지정
            .addPathPatterns("/board/**", "/user/**", "/reply/**", "/admin/**")
            // 인터셉터에서 제외할 URL 패턴 지정
            .excludePathPatterns(
                    "/login",
                    "/join",
                    "/logout",
                    "/board/list",
                    "/",
                    "/board/{id:\\d+}",
                    "/css/**",
                    "/js/**",
                    "/images/**",
                    "/favicon.ico",
                    "/h2-console/**"
            );
    
    // AdminInterceptor를 관리자 전용 페이지에 등록
    // 주의: LoginInterceptor 이후에 실행되므로 로그인은 이미 확인된 상태입니다.
    registry.addInterceptor(adminInterceptor)
            // 관리자 전용 페이지에만 적용
            .addPathPatterns("/admin/**");
}
```

**핵심 포인트**:
- `/admin/**` 경로는 `LoginInterceptor`와 `AdminInterceptor` 두 개의 인터셉터가 처리
- **인터셉터 실행 순서**: `LoginInterceptor` → `AdminInterceptor` → 컨트롤러
- 로그인 체크와 관리자 권한 체크를 인터셉터에서 처리하여 관심사 분리

---

### 4-2. AdminInterceptor - 관리자 권한 체크

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/interceptor/AdminInterceptor.java`

```java
/**
 * 관리자 인가 인터셉터
 * 
 * 관리자 전용 페이지(/admin/**)에 접근할 때 관리자 권한을 확인합니다.
 * 로그인은 LoginInterceptor에서 이미 확인했으므로, 여기서는 관리자 권한만 확인합니다.
 */
@Component
public class AdminInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler)
            throws Exception {
        HttpSession session = request.getSession();
        User sessionUser = (User) session.getAttribute("sessionUser");
        
        // 1. 로그인 체크는 LoginInterceptor가 이미 했으므로 생략 가능하지만, 안전을 위해 한번더 확인 
        if (sessionUser == null) {
            throw new Exception403("로그인이 필요합니다");
        }
        
        // 2. 관리자 권한이 없으면 인가 오류 발생
        if (!sessionUser.isAdmin()) {
            throw new Exception403("관리자 권한이 필요합니다");
        }
        
        // 관리자 권한이 있으면 컨트롤러로 진입 허용
        return true;
    }
}
```

**핵심 포인트**:
- `LoginInterceptor` 이후에 실행되므로 로그인은 이미 확인된 상태
- 관리자 권한이 없으면 `Exception403` 발생
- 인터셉터에서 처리하므로 컨트롤러는 비즈니스 로직에만 집중 가능

---

### 4-3. AdminController - 관리자 전용 컨트롤러

**파일**: `src/main/java/org/example/demo_ssr_v1_1/admin/AdminController.java`

```java
/**
 * 관리자 전용 페이지 컨트롤러.
 *
 * - /admin/** URI는 WebMvcConfig에서 LoginInterceptor와 AdminInterceptor가 처리합니다.
 * - 로그인 및 관리자 권한 체크는 인터셉터에서 자동으로 처리되므로 컨트롤러에서는 불필요합니다.
 */
@Controller
@RequiredArgsConstructor
public class AdminController {

    @GetMapping("/admin/dashboard")
    public String dashboard(HttpSession session, Model model) {
        // 로그인 및 관리자 권한 체크는 AdminInterceptor에서 이미 처리됨
        // 여기서는 sessionUser가 null이 아니고 관리자 권한이 있는 것이 보장됨
        User sessionUser = (User) session.getAttribute("sessionUser");
        
        model.addAttribute("adminName", sessionUser.getUsername());
        return "admin/dashboard";
    }
}
```

**핵심 포인트**:
- 로그인 및 관리자 권한 체크는 인터셉터에서 처리
- 컨트롤러는 비즈니스 로직에만 집중 (관심사 분리)
- **2단계 인가 체크**: `LoginInterceptor` (로그인) + `AdminInterceptor` (관리자 권한)

---

## 5. 헤더 메뉴 및 관리자 대시보드 뷰

### 5-1. header.mustache - 관리자 메뉴 및 역할 배지

**파일**: `src/main/resources/templates/layout/header.mustache`

```mustache
{{#sessionUser}}
    <li class="nav-item">
        <a class="nav-link" href="/board/save">글쓰기</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="/user/detail">회원정보보기</a>
    </li>
    {{#isAdmin}}
    <li class="nav-item">
        <a class="nav-link" href="/admin/dashboard">관리자</a>
    </li>
    {{/isAdmin}}
    <li class="nav-item">
        <a class="nav-link" href="/logout">로그아웃</a>
    </li>
{{/sessionUser}}

{{^sessionUser}}
    <li class="nav-item">
        <a class="nav-link" href="/join">회원가입</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="/login">로그인</a>
    </li>
{{/sessionUser}}

<!-- 오른쪽 역할/이름 영역 -->
<ul class="navbar-nav ms-auto">
    {{#sessionUser}}
    <li class="nav-item d-flex align-items-center">
        <span class="badge bg-secondary me-2">{{roleDisplay}}</span>
        <span class="text-light">{{username}} 님</span>
    </li>
    {{/sessionUser}}
</ul>
```

**핵심 포인트**:
- `{{#isAdmin}}` ... `{{/isAdmin}}`
  - 관리자일 때만 "관리자" 메뉴가 보이도록 제어
- `{{roleDisplay}}`
  - `ADMIN` 또는 `USER` 문자열을 뱃지 형태로 표시

---

### 5-2. 관리자 대시보드 뷰

**파일**: `src/main/resources/templates/admin/dashboard.mustache`

```mustache
{{> layout/header}}

<div class="container p-5">
    <div class="card">
        <div class="card-header">
            <h2 class="mb-0">관리자 대시보드</h2>
        </div>
        <div class="card-body">
            <p class="mb-1">안녕하세요, <b>{{adminName}}</b> 님.</p>
            <p class="text-muted mb-0">이 페이지는 관리자만 접근 가능합니다.</p>
        </div>
    </div>
</div>

{{> layout/footer}}
```

---

## 6. 초기 데이터 (data.sql)

**파일**: `src/main/resources/db/data.sql`

```sql
-- User 테이블 데이터 (5명의 사용자)
INSERT INTO user_tb (username, password, email, created_at) VALUES
  ('admin', '1234', 'admin@blog.com', NOW()),
  ('ssar', '1234', 'ssar@nate.com', NOW()),
  ('cos', '1234', 'cos@gmail.com', NOW()),
  ('hong', '1234', 'hong@naver.com', NOW()),
  ('kim', '1234', 'kim@daum.net', NOW());

-- UserRole 테이블 데이터 (사용자 역할: ADMIN / USER)
INSERT INTO user_role_tb (role, user_id) VALUES
  ('ADMIN', 1), ('USER', 1),  -- admin 계정: ADMIN + USER 둘 다
  ('USER', 2),                 -- ssar 계정: USER만
  ('USER', 3),                 -- cos 계정: USER만
  ('USER', 4),                 -- hong 계정: USER만
  ('USER', 5);                 -- kim 계정: USER만
```

**핵심 포인트**:
- `admin` 계정(id=1) → `ADMIN` + `USER` 두 역할 부여
- 나머지 계정 → `USER` 역할만 부여

---

## 💡 학생들을 위한 개념 설명

### 1. ROLE이란?

**"이 사용자가 어떤 일을 할 수 있는지"**를 나타내는 **권한 이름**입니다.

**예시**:
- `ADMIN`: 관리자 페이지, 설정, 회원 관리
- `USER`: 일반 기능 사용 (글쓰기, 댓글 등)

**실무에서는**:
- `MANAGER`, `GUEST`, `EDITOR` 등 다양한 ROLE을 정의합니다.

---

### 2. 왜 enum + UserRole 테이블을 함께 쓰나요?

**enum(Role)**:
- 문자열 `"ADMIN"` `"USER"` 대신 **타입 안전한** 방식
- 오타를 컴파일 타임에 잡을 수 있음

**별도 테이블(UserRole)**:
- 한 유저가 **여러 역할**을 가질 수 있게 하기 위해
- 예: `admin` 사용자는 ADMIN + USER 둘 다 가짐

**즉, "유저 1명 vs 역할 N개"의 구조**를 표현하기 역할을 저장하는 **별도의 테이블 둔 것입니다.


---

### 3. EAGER vs LAZY (학습용 간단 설명)

**LAZY (지연 로딩)**:
- "나중에 필요하면 그때 DB에서 가져와."
- 장점: 처음에 가볍게 가져옴
- 단점: 트랜잭션이 끝난 후에 접근하면 에러(LazyInitializationException)가 날 수 있음

**EAGER (즉시 로딩)**:
- "User를 가져올 때 roles도 같이 가져와."
- 장점: 뷰에서 바로 써도 안전함 (이번 예제에서 사용한 방식)
- 단점: 데이터가 매우 많을 때는 비효율적일 수 있음 (하지만 ROLE은 보통 적음)

**이 프로젝트에서는 `EAGER`를 사용했습니다.**

---

### 4. 화면 제어 vs 서버 인가

**화면 제어 (헤더에서 관리자 메뉴 숨기기)**:
- `{{#isAdmin}}` 블록으로 메뉴를 숨겨도,
- 누군가 주소창에 `/admin/dashboard`를 직접 치면 시도는 가능합니다.

**서버 인가 (인터셉터에서 권한 체크)**:
- `AdminInterceptor`에서 `sessionUser.isAdmin()`을 확인하여 접근을 차단합니다.
- 화면 + 서버 두 군데에서 **같은 RULE**을 적용해야 합니다.

**보안 원칙**: "화면은 사용자 편의를 위한 것이고, 진짜 보안은 서버에서 해야 한다."

**인터셉터 사용의 장점**:
- 관심사 분리: 인증/인가 로직을 컨트롤러에서 분리
- 재사용성: 여러 컨트롤러에 동일한 보안 정책 적용 가능
- 유지보수성: 보안 정책 변경 시 인터셉터만 수정하면 됨

---

## 테스트 시나리오

### 1. 관리자 계정 (admin / 1234)

**로그인 후**:
- 헤더 오른쪽 배지: `ADMIN`
- 네비게이션 바에 "관리자" 메뉴 표시
- `/admin/dashboard` 접속 → "관리자 대시보드" 페이지 정상 표시

### 2. 일반 사용자 (ssar / 1234 등)

**로그인 후**:
- 헤더 오른쪽 배지: `USER`
- 네비게이션 바에 "관리자" 메뉴 없음
- 주소창에 `/admin/dashboard` 입력 → 403 에러 (Exception403 "관리자만 접근 가능합니다")

### 3. 비로그인 사용자

- `/admin/dashboard` 접근 → 401 에러 (Exception401 "로그인이 필요합니다")

---

## 주요 파일 변경 목록

1. **`Role.java`** (신규)
   - `ADMIN`, `USER` enum 정의

2. **`UserRole.java`** (신규)
   - `user_role_tb` 엔티티
   - `(user_id, role)` 유니크 제약 조건

3. **`User.java`** (수정)
   - `roles: List<UserRole>` 필드 추가
   - `@OneToMany(cascade = ALL, orphanRemoval = true, fetch = EAGER)`
   - `addRole`, `hasRole`, `isAdmin`, `getIsAdmin`, `getRoleDisplay` 메서드 추가

4. **`UserRepository.java`** (수정)
   - `findByUsernameAndPasswordWithRoles()` 메서드 추가 (LEFT JOIN FETCH)

5. **`UserService.java`** (수정)
   - 로그인 시 역할 정보까지 함께 조회하도록 수정
   - 회원가입 시 기본 USER 역할 자동 추가 (`user.addRole(Role.USER)`)

6. **`WebMvcConfig.java`** (수정)
   - `LoginInterceptor` 적용 경로에 `/admin/**` 추가
   - `AdminInterceptor` 등록 및 `/admin/**` 경로에 적용

7. **`AdminInterceptor.java`** (신규)
   - 관리자 권한 체크 인터셉터
   - `/admin/**` 경로 접근 시 관리자 권한 확인
   - 관리자가 아니면 `Exception403` 발생

8. **`AdminController.java`** (신규)
   - `/admin/dashboard` 관리자 전용 컨트롤러
   - 로그인 및 관리자 권한 체크는 인터셉터에서 처리하므로 컨트롤러는 비즈니스 로직만 담당

9. **`MyExceptionHandler.java`** (수정)
   - 코드 설명에서 이모지 제거
   - `Exception400`, `Exception404`: `Model` 사용으로 통일 (`request.setAttribute` → `model.addAttribute`)
   - `Exception401`: `ResponseEntity` 방식으로 변경 (JavaScript alert 후 로그인 페이지로 이동)
   - `Exception403`: `ResponseEntity` 방식으로 변경 (JavaScript alert 후 이전 페이지로 이동)
   - `Exception500`, `DataIntegrityViolationException`, `RuntimeException`: `Model` 사용으로 통일
   - JavaScript 메시지 이스케이프 처리 (XSS 방지)

10. **`layout/header.mustache`** (수정)
    - `{{#isAdmin}}` 블록으로 관리자 메뉴 노출/숨김
    - `{{roleDisplay}}`로 역할 배지 표시

11. **`admin/dashboard.mustache`** (신규)
    - 관리자 전용 대시보드 화면

12. **`db/data.sql`** (수정)
    - `admin` 계정 및 `user_role_tb` 초기 데이터 삽입

---

## 🎯 핵심 정리

### ROLE 기반 권한 관리의 핵심

1. **Role enum**: 타입 안전한 역할 정의
2. **UserRole 테이블**: 한 유저가 여러 역할을 가질 수 있게 함
3. **EAGER 로딩**: 뷰에서 안전하게 권한 체크 가능
4. **2단계 인가**: 로그인 체크(LoginInterceptor) + 권한 체크(AdminInterceptor)
5. **화면 + 서버**: 둘 다에서 권한 체크 (보안)
6. **인터셉터 활용**: 관심사 분리로 코드 유지보수성 향상
7. **회원가입 시 기본 권한**: 신규 회원은 자동으로 USER 역할 부여
8. **예외 처리 개선**: ResponseEntity를 활용한 사용자 친화적 에러 처리

### 실무에서 중요한 점

- **보안**: 화면 제어만으로는 안전하지 않음 → 서버에서도 반드시 체크
- **확장성**: 나중에 `MANAGER`, `EDITOR` 등 역할 추가 가능
- **유연성**: 한 유저가 여러 역할을 가질 수 있음 (예: ADMIN + USER)
- **성능**: ROLE 데이터는 적으므로 EAGER 로딩도 문제없음
- **유지보수**: 역할 관련 로직을 User 엔티티에 모아서 응집도 높임
- **인터셉터 활용**: 인증/인가 로직을 인터셉터로 분리하여 관심사 분리

---

## 7. 예외 처리 개선 (MyExceptionHandler)

### 7-1. 예외 처리 방식 개선

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/errors/MyExceptionHandler.java`

#### 주요 변경사항

1. **Model 사용으로 통일**
   - `Exception400`, `Exception404`: `request.setAttribute` → `model.addAttribute`로 변경
   - `Exception500`, `DataIntegrityViolationException`, `RuntimeException`: `Model` 사용으로 통일

2. **ResponseEntity 방식 도입**
   - `Exception401`: JavaScript alert 후 로그인 페이지로 이동
   - `Exception403`: JavaScript alert 후 이전 페이지로 이동

3. **보안 강화**
   - JavaScript 메시지의 작은따옴표 이스케이프 처리 (XSS 방지)
   - Null 안전성 처리

#### Exception401 처리

```java
@ExceptionHandler(Exception401.class)
public ResponseEntity<String> ex401(Exception401 e, HttpServletRequest request) {
    log.warn("=== 401 에러 발생  ===");
    log.warn("요청 URL : {}", request.getRequestURL());
    log.warn("에러 메세지 : {}", e.getMessage());
    log.warn("예외 클래스 : {}", e.getClass().getSimpleName());

    // 자바스크립트 생성: alert 띄우고 -> location.href로 이동
    // 메시지의 작은따옴표를 이스케이프 처리
    String message = e.getMessage() != null ? e.getMessage() : "인증이 필요합니다";
    String escapedMessage = message.replace("'", "\\'");
    String script = "<script>" +
            "alert('" + escapedMessage + "');" +
            "location.href='/login';" +
            "</script>";

    return ResponseEntity
            .status(HttpStatus.UNAUTHORIZED) // 401 상태 코드 설정
            .contentType(MediaType.TEXT_HTML) // HTML 응답임을 명시
            .body(script);
}
```

**핵심 포인트**:
- 로그인하지 않은 사용자가 접근 시 자동으로 로그인 페이지로 이동
- 사용자 경험 개선: 에러 페이지 대신 alert 후 자동 리다이렉트

#### Exception403 처리

```java
@ExceptionHandler(Exception403.class)
@ResponseBody
public ResponseEntity<String> ex403(Exception403 e) {
    // 메시지의 작은따옴표를 이스케이프 처리
    String message = e.getMessage() != null ? e.getMessage() : "접근 권한이 없습니다";
    String escapedMessage = message.replace("'", "\\'");
    String script = "<script>alert('"+escapedMessage+"');" +
            "history.back();" +
            "</script>";

    return ResponseEntity
            .status(HttpStatus.FORBIDDEN)
            .contentType(MediaType.TEXT_HTML)
            .body(script);
}
```

**핵심 포인트**:
- 관리자 권한이 없는 사용자가 접근 시 alert 후 이전 페이지로 이동
- 사용자가 어디서 왔는지 기억하여 자연스러운 흐름 제공

---

## 8. 회원가입 시 기본 권한 부여

### 8-1. UserService 회원가입 메서드

**파일**: `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java`

```java
@Transactional
public User 회원가입(UserRequest.JoinDTO joinDTO) {
    // ... 유효성 검사, 중복 체크, 프로필 이미지 처리 ...
    
    // 4. DTO를 엔티티로 변환 (파일명 포함)
    User user = joinDTO.toEntity(profileImageFilename);

    // 5. 기본 권한 추가 (일반 사용자)
    // 회원가입 시 기본적으로 USER 역할을 부여합니다.
    user.addRole(Role.USER);

    // 6. JpaRepository의 save() 메서드: 엔티티 저장 (INSERT)
    return userRepository.save(user);
}
```

**핵심 포인트**:
- 회원가입 시 자동으로 `USER` 역할이 추가됨
- `CascadeType.ALL` 설정으로 `userRepository.save(user)` 호출 시 `UserRole`도 함께 저장됨
- 모든 신규 회원은 기본적으로 일반 사용자 권한을 가짐

